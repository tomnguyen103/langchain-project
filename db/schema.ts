/**
 * Drizzle schema barrel.
 *
 * Domain tables are added under `db/schema/*` starting in Goal 2
 * (social_accounts, posts, post_targets, media_assets, schedules), then
 * generated_content (Goal 4), research_topics (Goal 5), usage (Goal 6),
 * auto_reply_rules + comment_events (Goal 7). Re-export each here so
 * `import * as schema from "@/db/schema"` remains the single entry point.
 */
export {};
