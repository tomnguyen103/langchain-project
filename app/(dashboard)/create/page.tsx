import { PagePlaceholder } from "@/components/shared/page-placeholder";

export default function CreatePage() {
  return (
    <PagePlaceholder
      title="Create"
      description="Compose a post, attach media, pick platforms and schedule it."
      note="The composer and scheduler arrive in Goal 2."
    />
  );
}
