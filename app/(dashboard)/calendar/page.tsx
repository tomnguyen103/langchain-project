import { PagePlaceholder } from "@/components/shared/page-placeholder";

export default function CalendarPage() {
  return (
    <PagePlaceholder
      title="Calendar"
      description="See every scheduled and published post in one view."
      note="The scheduling calendar arrives in Goal 2."
    />
  );
}
