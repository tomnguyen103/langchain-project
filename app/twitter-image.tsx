// Reuse the Open Graph card for the Twitter/X summary_large_image so both
// previews stay identical without duplicating the design.
export { default, alt, size, contentType } from "./opengraph-image";
